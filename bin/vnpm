#! /bin/bash

# npm registry
NPM_REGISTRY=https://r-npm.3weijia.com

# node.js dist
NODE_DIST=https://npm.taobao.org/dist

npm --registry=$NPM_REGISTRY --disturl=$NODE_DIST \
    --userconfig=$HOME/.vnpmrc  --cache=$HOME/.cache/vnpm \
    $@